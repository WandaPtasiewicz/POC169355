package artGallery;

import java.util.Comparator;

public class TitleComparator implements Comparator<TitleComparator> {
    @Override
    public int compare(TitleComparator o1, TitleComparator o2) {
        return 0;
    }
}
